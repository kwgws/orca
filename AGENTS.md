# AGENTS instructions

## Coding style

- Use Python 3.12 features.
- Format code with `ruff --fix`. The `pyproject.toml` config is used.

## Validation

- Run `ruff --fix .` before each commit to ensure lint and tests pass.

## Commit messages

- Keep messages concise but descriptive.
