[chat]
model = "llama3"
base_url = "${OLLAMA_URL}"
temperature = 0.7

[router]
model = "phi3:3.8b"
base_url = "${OLLAMA_URL}"
temperature = 0.2

[reranker]
model = "mistral:7b-instruct"
base_url = "${OLLAMA_URL}"
temperature = 0.4

[searchifier]
model = "mistral:7b-instruct"
base_url = "${OLLAMA_URL}"
temperature = 0.2

[default]
model = "mistral:7b-instruct"
base_url = "${OLLAMA_URL}"